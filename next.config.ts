import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // transpilePackages: ['framer-motion'],
  images: {
    domains: ['instagram.fixc5-2.fna.fbcdn.net','via.placeholder.com','res.cloudinary.com','images.pexels.com'],
  
  },

};

export default nextConfig;
